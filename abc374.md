## <font color=green>ABCDEF</font>G

---

## Good

## E - Sensor Optimization Dilemma 2

思路：

貌似是个经典调整贪心，但我不会。

二分，问题转化为判断如何购入两种机器，使得产量 $\geq limit$ 且价格尽可能少。

考虑有一种购买方案，使得 $a\times ca + b\times cb \geq limit$ ，我们可以将 $b$ 个 $a$ 机器与 $a$ 个 $b$ 机器互换，即 $左式=a\times (ca - b) + b\times (cb + a) \geq limit$ ，价格增量为 $\Delta = (ca\times p + cb\times q) - ((ca - b)\times p + (cb + a)\times q) = b\times p - a \times q$ ，设 $\Delta > 0$ ，那么 $\frac{a}{p}<\frac{b}{q}$ 。

因此我们得到结论：对于性价比（产量 / 价格）更小的机器，我们可以将它替换为另一种机器，使得产量不变同时价格更小。

这样不断替换后，容易知道是一个 $ca$ 对 $b$ 的取模操作，最终 $0\leq ca < b$ 。可以直接枚举。

AC代码：[https://atcoder.jp/contests/abc374/submissions/58598848](https://atcoder.jp/contests/abc374/submissions/58598848)

---

## Fair

## F - Shipping

思路：

分析可得每一个 $T_i$ 产生的决策点一定不超过 $n$ 个，决策点一共有 $n^2$ 个。

因此可以划分dp，定义 $dp(i, j)$ 表示前 $i$ 个物品打包走后，且冷却期结束时间为 $j$ 的最小代价。枚举复杂度不太会算，感觉是对于每个 $i$ 的决策点有 $n^2$ 个，因此前缀中的决策点有 $n^3$ 个。转移复杂度为 $O(n^4)$

代码复杂度优化细节：第二维的时间值域非常大，因此可以开 $n$ 个 `unordered_map<LL, LL>` 来防止离散化。

AC代码：[https://atcoder.jp/contests/abc374/submissions/58481060](https://atcoder.jp/contests/abc374/submissions/58481060)