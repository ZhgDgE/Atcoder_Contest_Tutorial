## <font color=green>AB</font>CDE<font color=green>FG</font>Ex

---

## Good

## F - Octopus

思路：

决策点一共有 $n^2$ 个，可以处理出来，排序。然后我们在一维坐标系上观察，发现决策点把坐标系划分为了若干段，对于每个决策点 $cand_i$ ，可能有 $ok(cand_i) \neq ok(cand_i \pm 1)$ 。另外两个无穷长度的段总为 `false` 。

那么如何计算？我们先只考虑以决策点为开区间端点的段，这些段段内的 `ok` 值总是不变的。然后再单独考虑决策点上的 `ok` 值。

时间复杂度 $O(n^3\times \log n)$ 

AC代码：[https://atcoder.jp/contests/abc318/submissions/58607454](https://atcoder.jp/contests/abc318/submissions/58607454)

---

## Good

## G - Typical Path Problem

思路：

[圆方树](https://oi-wiki.org/graph/block-forest/)。先介绍一些定义。

- 割点：删掉之后联通块数量增加的点。
- 点双（原始定义）：图中任意两不同点之间都有至少两条点不重复的路径。
- 点双（近似定义）：不存在割点的图。这个定义只在图中只有两个点，一条连接它们的边时失效。它没有割点，但是并不能找到两条不相交的路径，因为只有一条路径。

我们建立圆方树时用的是近似定义。

建立：

- 我们知道，一个点成为割点时，会出现在至少两个点双中。在圆方树中，原来的每个点对应一个 **圆点**，每一个点双对应一个 **方点**。而对于每一个点双连通分量，它对应的方点向这个点双连通分量中的每个点连边。这样圆方树就建好了。

性质：

说到 **简单路径**，就必须提一个关于点双很好的性质：**对于一个点双中的两点，它们之间简单路径的并集，恰好完全等于这个点双。**

即同一个点双中的两不同点 u,v 之间一定存在一条简单路径经过给定的在同一个点双内的另一点 w。

这个结论能告诉我们什么呢？它告诉了我们：**考虑两圆点在圆方树上的路径，与路径上经过的方点相邻的圆点的集合，就等于原图中两点简单路径上的点集。**

---

回到这题。我们需要判断的是，$A\rightarrow C$ 的所有简单路径中，是否有 $B$ ，即判断圆方树上经过的方点中，是否包含 $B$ 。

如何做多次查询？我们想要枚举 $B$ 所在方点，判断点是否在路径上，容易知道方点数量会被卡到 $O(n)$ 级别。优化？题解给的思路是，离线下来挂到 $B$ 点上，就是均摊的复杂度，然后点染色，并树剖查询路径上的染色数量。

其实可以不用树剖。我们先判断 $ancestor(b)$ 是否在路径上，然后判断是否有某个 $b$ 的儿子节点在路径上（圆点旁边一定是方点）。如果 $lca(a, c)$ 的深度大于 $b$ 儿子，一定不经过。否则，只需要判断 $a, c$ 某个点是否在 $b$ 子树内即可。

AC代码：[https://atcoder.jp/contests/abc318/submissions/58791576](https://atcoder.jp/contests/abc318/submissions/58791576)

---

这题是 判断两点间是否存在简单路径**经过**某点，有一道题是 判断两点间是否存在简单路径**不经过**某点。

如何做？其实更简单，圆方树上一条以圆点为端点的路径，非端点一定是割点（连接了至少两个点双），如果路径经过了 $B$ ，那么路径必须经过 $B$ 。

AC代码：[https://vjudge.net/solution/55019243](https://vjudge.net/solution/55019243)